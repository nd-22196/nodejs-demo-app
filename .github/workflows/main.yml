name: CI/CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Build Docker image
        run: docker build -t ${{ secrets.snd3226  }}/nodejs-demo-app:latest .

      - name: Log in to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.snd3226 }}
          password: ${{ secrets.3w6HQ5agTTLdeUkX-VOA6z6boE }}

      - name: Push Docker image to DockerHub
        run: docker push ${{ secrets.snd3226 }}/nodejs-demo-app:latest